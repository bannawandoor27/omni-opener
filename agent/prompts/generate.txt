You are building a client-side JavaScript tool for OmniOpener (omniopener.dev).

Create a new file at {{SCRIPT_PATH}} that implements a browser-based {{FORMAT}} file viewer/converter.

IMPORTANT: Use the OmniTool SDK (window.OmniTool). It is pre-loaded by the router.

STRICT REQUIREMENTS:
1. Wrap ALL code in an IIFE: (function(){ ... })();
2. Define window.initTool = function(toolConfig, mountEl) { ... } inside the IIFE
3. Inside initTool, call OmniTool.create(mountEl, toolConfig, { ... }) with these options:

   OmniTool.create(mountEl, toolConfig, {
     accept: '.ext1,.ext2',           // file extensions
     dropLabel: 'Drop a .xxx file',   // drop zone label
     binary: false,                    // true for binary files (images, zip, etc.)
     infoHtml: '<strong>...</strong>', // info footer
     actions: [                        // toolbar buttons
       { label: '📋 Copy', id: 'copy', onClick: function(helpers, btn) { ... } },
       { label: '📥 Download', id: 'dl', onClick: function(helpers, btn) { ... } },
     ],
     onInit: function(helpers) {
       // Load CDN deps: helpers.loadScript(url, callback)
       // Load CSS: helpers.loadCSS(url)
     },
     onFile: function(file, content, helpers) {
       // content = file text (or ArrayBuffer if binary:true)
       // helpers.render(html)      — show rendered output
       // helpers.showLoading(msg)  — show spinner
       // helpers.showError(title, detail) — show error
       // helpers.download(filename, content, mime) — trigger download
       // helpers.copyToClipboard(text, btn) — copy + button feedback
       // helpers.getRenderEl()    — get the render container DOM element
       // helpers.loadScript(url, cb) — load CDN dynamically
     }
   });

4. Do NOT implement drag-drop, file input, error states manually — the SDK handles all of that.
5. Load any CDN dependencies dynamically in onInit using helpers.loadScript()
6. Parse and render the {{FORMAT}} file content in onFile
7. Include meaningful actions (copy, download converted, etc.)
8. Use Tailwind classes: surface-50/100/200/..., brand-50/100/200/...
9. Output ONLY the JavaScript file, no explanations

Reference the existing tools in {{ROOT}}/public/tools/ for patterns. Read {{ROOT}}/public/tool-sdk.js for the full API.
