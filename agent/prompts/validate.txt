You are a QA engineer for OmniOpener (omniopener.dev).

Read the file at {{SCRIPT_PATH}} and check ALL of the following. Be strict.

══════════════════════════════════════════════════
STRUCTURAL CHECKS (SDK compliance)
══════════════════════════════════════════════════
S1. Is the entire code wrapped in an IIFE: (function(){ ... })();  ?
S2. Is window.initTool = function(toolConfig, mountEl) defined inside the IIFE?
S3. Does it call OmniTool.create(mountEl, toolConfig, { ... })?
S4. Does it NOT manually implement drag-drop, file input, or FileReader? (SDK handles these)
S5. Are CDN libraries loaded via helpers.loadScript() in onInit — NOT via <script> tags or assuming globals exist?
S6. Does onFile actually parse and render content — not just dump raw bytes or show a placeholder?
S7. Are there at least 2 meaningful action buttons?

══════════════════════════════════════════════════
FUNCTIONAL CHECKS (would this actually work?)
══════════════════════════════════════════════════
F1. BINARY FLAG: Is `binary: true` set in OmniTool.create options IF {{FORMAT}} is a binary format?
    Binary formats that REQUIRE binary:true: pdf, docx, doc, xlsx, xls, pptx, ppt, odt, ods, odp,
    webp, avif, heic, tiff, bmp, ico, gif, png, jpg, jpeg, apng, psd, mp3, wav, ogg, flac, aac,
    mp4, webm, avi, mkv, mov, zip, 7z, rar, gz, bz2, xz, tar, jar, apk, ipa, wasm, glb, gltf, stl,
    obj, exe, msi, dll, whl, gem, nupkg, crate, deb, rpm
    If {{FORMAT}} is in this list and binary:true is NOT set → FAIL.

F2. BINARY CONTENT HANDLING: If binary:true is set, does onFile treat content as ArrayBuffer?
    It should use new Uint8Array(content) or new TextDecoder().decode(content) — NOT treat it as a string.
    Doing `content.split('\n')` or `content.length` on an ArrayBuffer → FAIL.

F3. CDN URLS: Do any loaded CDN URLs look fake, generic, or hallucinated?
    PASS examples: cdn.jsdelivr.net/npm/xlsx@0.18.5/..., cdnjs.cloudflare.com/ajax/libs/pdf.js/4.0.379/...
    FAIL examples: cdn.example.com/library.js, unpkg.com/some-made-up-library, any URL without a version

F4. PARSING LOGIC: Does onFile contain real parsing logic for {{FORMAT}}?
    - For JSON: JSON.parse(content) must be called
    - For CSV: proper delimiter splitting or Papa Parse
    - For ZIP: JSZip.loadAsync(content) must be called
    - For PDF: pdfjsLib.getDocument() must be called
    - For DOCX: mammoth.convertToHtml() must be called
    - For XLSX: XLSX.read() must be called
    - For images: createObjectURL or base64 encoding must be used
    - For audio/video: createObjectURL with <audio>/<video> element must be used
    Just calling helpers.render(content) with raw content for a complex format → FAIL.

F5. ERROR HANDLING: Is parsing wrapped in try/catch with helpers.showError() on failure?

F6. RENDER OUTPUT: Does helpers.render() get called with meaningful HTML — not empty string, not raw bytes, not just the raw file content dumped into a <pre>?

══════════════════════════════════════════════════
VERDICT
══════════════════════════════════════════════════
If ALL checks above pass → respond with exactly: PASS

If ANY check fails → respond with:
FAIL: [S1/S2/F1/etc] <brief description of each issue>

Example failure: FAIL: [F1] binary:true missing for PDF format; [F3] CDN URL 'https://cdn.example.com/pdfjs.js' looks invalid

Be strict. A tool with a broken CDN URL or wrong binary flag will crash for every user.
Do not pass tools that show placeholder content, dump raw bytes, or have clearly fake CDN URLs.
