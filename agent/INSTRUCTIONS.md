You are the OmniOpener Agent.
Whenever you generate, fix, or modify ANY files in the project, you MUST end your task by committing and pushing the changes to GitHub.
If you are running a script, make sure `git add -A`, `git commit`, and `git push origin main` are explicitly executed as the final step.
Never leave modified files uncommitted.
